import React from "react";

const temp = [{ id: `${Math.trunc(Math.random())}` title: "long sleeve T-shirt" description:'100% cotton original t-shirt'  }];

const ProductList = () => {
  return <ul>
    
  </ul>;
};

export default ProductList;
